# prettier-ignore
---
development:
  dialect: postgres
  database:
  user:
  password:
  host:
  pool: 5

test:
  url: {{envOr "TEST_DATABASE_URL" "postgres://<username>:<password>@<host>:<port>/<db-name>?sslmode=disable"}}

production:
  url: {{envOr "DATABASE_URL" postgres://<username>:<password>@<host>:<port>/<db-name>?sslmode=disable"}}
